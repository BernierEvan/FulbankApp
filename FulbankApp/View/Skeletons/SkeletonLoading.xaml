<UserControl x:Class="FulbankApp.View.Skeletons.SkeletonLoading"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="800"
             Background="Transparent">
    <Grid Background="Transparent">
        <Grid HorizontalAlignment="Center"
              VerticalAlignment="Center"
              MaxWidth="1920"
              Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="{Binding Message, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontSize="48"
                       FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,30"/>

            <StackPanel Grid.Row="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,0,0,40">
                <Border Style="{StaticResource SkeletonBlockStyle}" Width="240" Height="48" Margin="12,0"/>
                <Border Style="{StaticResource SkeletonBlockStyle}" Width="260" Height="48" Margin="12,0"/>
                <Border Style="{StaticResource SkeletonBlockStyle}" Width="220" Height="48" Margin="12,0"/>
                <Border Style="{StaticResource SkeletonBlockStyle}" Width="200" Height="48" Margin="12,0"/>
                <Border Style="{StaticResource SkeletonBlockStyle}" Width="180" Height="48" Margin="12,0"/>
            </StackPanel>

            <UniformGrid Grid.Row="2"
                         Rows="2"
                         Columns="2"
                         Margin="140,0,140,0"
                         HorizontalAlignment="Stretch"
                         VerticalAlignment="Stretch">
                <Border Style="{StaticResource SkeletonBlockStyle}" Height="200" Margin="20"/>
                <Border Style="{StaticResource SkeletonBlockStyle}" Height="200" Margin="20"/>
                <Border Style="{StaticResource SkeletonBlockStyle}" Height="200" Margin="20"/>
                <Border Style="{StaticResource SkeletonBlockStyle}" Height="200" Margin="20"/>
            </UniformGrid>
        </Grid>
    </Grid>
</UserControl>