<UserControl x:Class="FulbankApp.View.Skeletons.WalletSkeletonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Style="{StaticResource SkeletonBlockStyle}" 
                Width="200" Height="40" 
                HorizontalAlignment="Left" Margin="0,0,0,30"/>

        <Border Grid.Row="1" Style="{StaticResource SkeletonBlockStyle}" 
                Height="200" Margin="0,0,0,30"/>

        <StackPanel Grid.Row="2">
            <Border Style="{StaticResource SkeletonBlockStyle}" Height="60" Margin="0,0,0,10"/>
            <Border Style="{StaticResource SkeletonBlockStyle}" Height="60" Margin="0,0,0,10"/>
            <Border Style="{StaticResource SkeletonBlockStyle}" Height="60" Margin="0,0,0,10"/>
            <Border Style="{StaticResource SkeletonBlockStyle}" Height="60" Margin="0,0,0,10"/>
        </StackPanel>
    </Grid>
</UserControl>